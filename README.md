# HeartSync (Dating Swipe App)

A modern, high-performance dating swipe application for Windows.

## 🚀 How to Run on Windows

To launch the application on Windows, run the following command in your terminal:

```bash
flutter run -d windows
```

